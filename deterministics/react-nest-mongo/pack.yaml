apiVersion: v1
name: react-nest-mongo
displayName: React + NestJS + MongoDB
targets:
  frontend: react
  backend: nestjs
  database: mongodb
toolPolicyProfile: standard-openhands-webapp
entrypoints:
  commandsFile: commands.yaml
  gatesFile: gates.yaml
  repoHintsFile: repoHints.yaml
  promptsDir: prompts
  schemasDir: schemas
artifacts:
  spec: .ai/spec.json
  plan: .ai/plan.json
  audit: .ai/audit.json
  worklog: .ai/worklog.jsonl
  evidenceDir: .ai/evidence
  finalReport: .ai/final-report.md
